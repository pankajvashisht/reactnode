<!DOCTYPE html>
<html>
  <head>
    <title>Change Password - 2Tiger LLC</title>
    <link rel='stylesheet' href='<%= appURL %>css/bootstrap.min.css' />
  </head>
  <body>
    <div class="container-fluid text-center">    
        <div class="row content">
          <div class="col-sm-2 sidenav">
          </div>
          <div class="col-sm-8 text-left"> 
            <center><h1>Welcome 2Tiger LLC</h1>
            <h6 class="<%= (error)?'alert alert-danger':'alert alert-success' %>"> <%= message %></h6></center>
            <%if(!success || confirmPassword) { %>
                <div class="container">
                    <h2>Create New Password</h2>
                    <form method="post">
                      <div class="form-group">
                        <label for="email">New Password:</label>
                        <input type="password" class="form-control" required="true" id="email" placeholder="New Password" name="password">
                      </div>
                      <div class="form-group">
                        <label for="pwd">Confirm New Password:</label>
                        <input type="password" class="form-control" required="true" id="pwd" placeholder="Confirm New Password" name="confirm_password">
                      </div>
                      <button type="submit" class="btn btn-success">Create Password</button>
                    </form>
                  </div>
            <% } %>    
            <hr>
          </div>
          <div class="col-sm-2 sidenav">
          </div>
        </div>
      </div>
      
      <footer class="container-fluid text-center">
        <p>Copy right@<%= new Date().getFullYear() %></p>
      </footer>
      
      </body>
      </html>
      
  </body>
</html>